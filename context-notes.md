# 게시판 공유 기능 수정 작업 기록

- 현재 공유 아이콘 버튼에는 클릭 핸들러가 없다.
- 공유 URL은 기존 쿼리와 해시를 제거하고 `postId`만 포함한다.
- Web Share API가 있으면 시스템 공유창을 사용하고, 미지원 또는 호출 실패 시 클립보드 복사를 시도한다.
- 사용자가 시스템 공유창을 직접 취소한 경우 오류 토스트를 표시하지 않는다.
- 앱 최초 진입 시 유효한 `postId` 쿼리가 있으면 해당 게시글 상세 화면으로 이동한다.
- 공유 링크로 진입할 때 저장된 로그인 토큰이 있으면 사용자 정보도 함께 복원한다.
- 상세 화면에서 뒤로 갈 때 URL의 `postId` 쿼리를 제거한다.
- `npm run build`는 성공했다.
