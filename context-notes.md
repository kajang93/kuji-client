# OAuth 정상화 작업 기록

- `511dd9d4` 커밋에서 로그인 화면을 수정하면서 네이버·구글 OAuth 진입 로직이 제거되었다.
- 같은 커밋에서 카카오 키 참조가 실제 `.env`의 `VITE_KAKAO_REST_API_KEY`가 아닌 `VITE_KAKAO_CLIENT_ID`로 변경되었다.
- 네이버·구글 콜백 및 백엔드 로그인 API는 남아 있으므로 로그인 화면의 진입 로직만 최소 범위로 복원한다.
- 앱 환경은 딥링크가 준비되지 않았으므로 기존 제한을 유지한다.
- 사용자가 수정한 `README.md`는 이번 변경 및 커밋에서 제외한다.
- `npm run build`는 성공했다.
- `npm run typecheck`는 OAuth 변경과 무관한 기존 프로젝트 오류들로 실패했으며, 이번 변경에서 새로 추가된 타입 오류는 확인되지 않았다.
